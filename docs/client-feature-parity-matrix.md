# Client Feature Parity Matrix

Status: Active during Stage 16 and Stage 17 gate work

## Purpose

This matrix tracks the march from the current Godot protocol prototype toward a fully functional Godot-native WotLK client for AzerothCore.

The target is not a companion dashboard, a partial client, or a reimagined MMO. Every normal player-facing client feature needs either a working Godot implementation or an explicit compatibility note explaining what remains and why.

The full Stage 17 engine checklist is [WotLK Client Parity Engine Spec](wotlk_client_parity_engine_spec.md). This matrix is the progress ledger against that spec.

## Current Foundation

| Foundation Area | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Auth and realm connection | Working UI-integrated prototype | Stage 11 protocol helper authenticates and reaches realm/world flow. `scenes/game_login_view.tscn` now sends typed host/account/password into `run_character_flow(...)` and only advances after an authenticated roster result. | Harden retry/error handling, loading/cancel states, failure-code display, and long-session stability. |
| Character listing | Working UI-integrated prototype | Stage 11/12 helper can list characters. `scenes/character_select_view.tscn` now consumes the authenticated login roster from in-memory `SessionContext`, can refetch with the same typed credentials, and normalizes native-extension dictionaries plus helper-process `CHAR ...` rows. | Add full equipment preview, delete flow, account-driven create-character support, realm selection, and visual polish. |
| Enter world | Working UI-integrated prototype | Stage 12 enters the world and reads initial server state. The character-select UI now calls `enter_world(name, host, port, account, password)` for the selected character, stores the selected character/result in `SessionContext`, and routes successful login into `scenes/world_session_view.tscn`. The shell now consumes saved movement, camera, target, action, interact, reset, and jump keybindings for UI-side marker/HUD control; keeps Chat, Spells, Actions, Quests, Options, and Bags as independent in-world HUD panels instead of replacing the session scene; renders safe session inventory dictionaries in a 39-slot Bags grid when available; and persists movable/resizable HUD layout. | Replace the world-session shell with a persistent gameplay HUD/session that keeps the socket/session alive through broader feature usage and turns the UI input hooks/panels into server-synchronized gameplay. |
| Movement | Working prototype | Stage 13 sends movement and verifies drift. | Add complete movement modes, prediction, collisions, fall/swim/fly edge cases, and manual tests. |
| Object visibility | Working prototype | Stage 14 parses nearby creatures/gameobjects. | Expand update-field parsing, despawns, object values, dynamic objects, corpses, and players. |
| Targeting, interaction, combat probe | Working prototype | Stage 15 targets live object GUIDs, opens gossip, and starts combat. | Add complete combat loop, spells, range/facing feedback, death, regen, and loot. |

## Stage 16 Feature Areas

| Feature Area | Stage 16 Status | Protocol/Data Targets | Godot Surface | Validation Target | Current Notes |
| --- | --- | --- | --- | --- | --- |
| Chat | Working expanded slice | `CMSG_MESSAGECHAT`, `SMSG_MESSAGECHAT`, basic say type, self-whisper type, race language selection, server echo parse. | `scenes/stage16_chat_view.tscn` with chat log, mode selector, input, and send button. `scenes/world_session_view.tscn` now has a resident Chat panel that can render safe session chat rows without calling the bridge. | `ACORE_CHAT_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_chat_view.tscn` returns say echo and self-whisper/inform responses through opcode `0x096`. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Chat panel with synthetic chat rows. | Say and self-whisper round trips work, and a resident world-session Chat surface can display session rows. Remaining chat parity includes feeding live chat/system rows into the persistent HUD, true receiver-side whispers, channels, party/guild/raid chat, system filters, tabs, history, command parsing, and social integration. |
| Inventory | Working expanded Stage 17 slice | Player private update fields for equipped slots, bag slots, backpack slots, item GUIDs, coinage field detection, item object entry/stack/durability fields, `CMSG_ITEM_QUERY_SINGLE` / `SMSG_ITEM_QUERY_SINGLE_RESPONSE` template lookup, controlled `CMSG_SWAP_INV_ITEM` base-backpack move/restore, controlled `CMSG_SPLIT_ITEM` stack split/merge, and loot-to-inventory before/after snapshot comparison. | `scenes/stage17_inventory_view.tscn` with 39-slot equipment, bag, and backpack grid showing live item names, entries, stack counts, durability when present, plus test controls for backpack move, equipment unequip, and stack split. `scenes/stage17_loot_view.tscn` also refreshes an after-loot inventory list when the loot-to-bag handoff runs. `scenes/world_session_view.tscn` now has an in-session Bags panel that can render a safe 39-slot inventory grid from session dictionaries without calling the bridge. | `ACORE_INVENTORY_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_inventory_view.tscn` returns 39 slots, populated slots, item-detail rows, and resolved names for `Codexstage`. `ACORE_INVENTORY_SWAP_SELF_TEST=1` confirms swap/restore for slots `23` and `25`. `ACORE_STACK_SPLIT_SELF_TEST=1` confirms split/merge for slots `23` and `25`. `ACORE_LOOT_INVENTORY_SELF_TEST=1` now confirms exact-selected corpse loot changing inventory after pickup. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Bags panel with synthetic money, populated slot rendering, and a 39-slot grid. | Inventory slot details, reversible server-side item movement, reversible stack split/merge, exact-selected loot-to-bag handoff evidence, after-loot inventory refresh, and a resident world-session Bags surface now exist in the Godot client path. Remaining inventory parity includes feeding live snapshots into the persistent HUD, item icons/tooltips/stats, bag contents beyond backpack, player drag/drop, item use/destroy/equip variants, item cooldown/failure UI, nested bag mutation, full-bag handling, and broader persistence checks after item actions. |
| Equipment | Working expanded Stage 17 slice | Equipment slots, item detail parsing, durability fields, and `CMSG_SWAP_INV_ITEM` unequip/restore from equipment slot `15` to backpack slot `26`. | Stage 17 inventory scene currently exposes a safe `Test Unequip` control. `scenes/world_session_view.tscn` now has a resident Character panel with a 19-slot equipment grid using safe session inventory summaries. | `ACORE_EQUIPMENT_SWAP_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_inventory_view.tscn` confirms unequip and restore for slots `15` and `26`. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Character panel with profile, money, and equipment-slot rows. | Godot can now perform one reversible equipment mutation against live AzerothCore state and display a first paper-doll style equipment surface in the active world HUD. Remaining equipment parity includes live refresh into the persistent HUD after mutations, item icons/tooltips/stats, arbitrary equip/unequip drag/drop, item requirement validation, stat/aura recalculation display, weapon/offhand constraints, failure-code UI, durability repair flows, and persistence checks after normal player-driven equipment changes. |
| Targeting and combat | Working expanded slice | `visible_targets_snapshot`, exact runtime GUID selectors for loot/combat scene probes, `CMSG_SET_SELECTION`, `CMSG_ATTACKSWING`, `CMSG_ATTACKSTOP`, `SMSG_ATTACKSTART`, `SMSG_ATTACKSTOP`, `SMSG_ATTACKERSTATEUPDATE`, time-sync response, movement approach/facing. | `scenes/interaction_combat_view.tscn` with live NPC interaction plus parsed combat feedback. `scenes/stage17_loot_view.tscn` can scan live visible creature objects, choose a live combat-safe target, and send that exact GUID into corpse-loot and loot-to-bag probes. | `ACORE_INTERACTION_COMBAT_SELF_TEST=1 godot-4 --headless --path . res://scenes/interaction_combat_view.tscn` receives `SMSG_GOSSIP_MESSAGE` opcode `0x17D` and parsed `SMSG_ATTACKERSTATEUPDATE` opcode `0x14A`. `ACORE_LOOT_TARGET_PICKER_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_loot_view.tscn` passed with 140 visible creature targets and selected exact GUID `0xf130000045000daa` for entry `69`. | Godot now parses melee damage state from a live hostile target and has a reusable visible-target snapshot path for exact-GUID target selection in the loot scene. Remaining combat parity includes real in-world click picking, health deltas, threat, death, loot transition, ranged combat, spell combat consequences, swing timers, auto-repeat combat state, and target/unit frames. |
| Loot | Working expanded Stage 17 slice | `CMSG_LOOT`, `CMSG_LOOT_RELEASE`, `CMSG_LOOT_MONEY`, `CMSG_AUTOSTORE_LOOT_ITEM`, `SMSG_LOOT_RESPONSE`, `SMSG_LOOT_RELEASE_RESPONSE`, `SMSG_LOOT_REMOVED`, `SMSG_LOOT_MONEY_NOTIFY`, unit health fields, unit dynamic lootable flags, visible-target snapshot, exact GUID selector handoff, and inventory before/after comparison. | `scenes/stage17_loot_view.tscn` with visible target scan/picker, editable target controls, quick loot-open, fight-to-loot, loot-to-bag controls, changed-slot display, refreshed after-loot inventory list, and headless self-tests. `scenes/world_session_view.tscn` now has a resident Loot panel that can render safe session loot rows without calling the bridge. | `ACORE_LOOT_TARGET_PICKER_SELF_TEST=1` passes with a live exact target list. `ACORE_LOOT_OPEN_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_loot_view.tscn` passes with release response opcode `0x161` for a not-yet-killed target. `ACORE_CORPSE_LOOT_SELF_TEST=1` selected exact GUID `0xf130000045000dab`, killed entry `69`, saw `dead=true` and `lootable=true`, opened corpse loot with opcode `0x160`, received item removal confirmations, and released loot. `ACORE_LOOT_INVENTORY_SELF_TEST=1` selected exact GUID `0xf130000045000daa`, confirmed `changed_slots=1`, `added_slots=1`, and `handoff=true`. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Loot panel with synthetic loot money, item rows, and inventory changes. | Godot can now scan and choose live visible creature targets, send exact GUIDs into bounded kill-to-corpse and loot-to-bag flows, refresh visible inventory data after successful pickup, and display a first resident loot-window surface in the active world HUD. Remaining loot parity includes feeding live loot snapshots into the persistent HUD, exact in-world click targeting, integrated combat-to-loot UX, persistent sessions, item placement/full-bag handling, money-loot evidence with nonzero copper, group loot/rolls, corpse cleanup, loot error UI, and long-session persistence checks. |
| Vendors | Working expanded Stage 17 slice | `visible_targets_snapshot`, exact runtime GUID selector handoff, `CMSG_LIST_INVENTORY`, `SMSG_LIST_INVENTORY`, `CMSG_BUY_ITEM`, `SMSG_BUY_ITEM`, `SMSG_BUY_FAILED`, `CMSG_SELL_ITEM`, `SMSG_SELL_ITEM`, interaction-range movement, return movement, vendor item row parse, selected row metadata, quantity input, before/after inventory snapshots, bought-slot state display, and coinage snapshots. Repair packets remain future work. | `scenes/stage17_vendor_view.tscn` with visible target scan/picker, target controls, live server-returned item rows, selected slot/item/price/stock feedback, quantity input, selected-row `Buy + Sell`, transaction status, money deltas, and bought-slot before/after rows. `scenes/world_session_view.tscn` now has a resident Vendor panel that can render safe session vendor rows without calling the bridge. | Native `--vendor-list`, native `--vendor-buy-sell`, `ACORE_VENDOR_LIST_SELF_TEST=1`, `ACORE_VENDOR_BUY_SELL_SELF_TEST=1`, and UI-lane `ACORE_VENDOR_UI_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_vendor_view.tscn`. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Vendor panel with synthetic item rows and transaction feedback. | Godot can now open a live AzerothCore vendor list, perform a selected-row buy/sell round trip through the client protocol path, and display a first resident vendor-window surface in the active world HUD. A synthetic UI self-test also validates selected-row/transaction rendering without bridge or live-session code. Rows stay item-id based so no proprietary item names/icons/tooltips are committed. Remaining vendor parity includes feeding live vendor snapshots into the persistent HUD, persistent inventory panel refresh after purchase/sale, repair, stock/failure UI, local-only item metadata/icons/tooltips, in-world click targeting, and persistent-session integration. |
| Quests | Complete | Quest gossip, accept/complete, objective tracking, reward selection. | Quest detail, tracker, reward UI. | Accept and complete a simple test quest. | Requires database-assisted quest target selection. |
| Trainers | Working expanded Stage 17 slice | `visible_targets_snapshot`, exact runtime GUID selector handoff, `CMSG_TRAINER_LIST`, `SMSG_TRAINER_LIST`, `CMSG_TRAINER_BUY_SPELL`, `SMSG_TRAINER_BUY_FAILED`, `SMSG_TRAINER_BUY_SUCCEEDED`, trainer spell rows, server state values, money/level/skill/prerequisite requirements, proximity-gated NPC interaction movement, return movement, learned-spell verification, and coinage verification. | `scenes/stage17_trainer_view.tscn` with live visible-unit scan/picker, target controls, a live server-returned trainer spell list with clearer state/cost/requirement rows, a bounded `Try Learn` action showing success/failure response state, and a `Verify Learn` path that checks spellbook and money after a server-authoritative learn. `scenes/world_session_view.tscn` now has a resident Trainer panel that can render safe session trainer rows without calling the bridge. | `ACORE_TRAINER_TARGET_PICKER_SELF_TEST=1` selected entry `911` by exact GUID from 143 visible unit targets. Native `--trainer-list` returns opcode `0x1B1` and nonzero spell rows. Native `--trainer-buy` returns opcode `0x1B4` with failure reason `1` for the low-money fixture. `tools/prepare_trainer_buy_fixture.py` prepares the disposable local character, and `ACORE_TRAINER_BUY_SUCCESS_SELF_TEST=1` returns opcode `0x1B3`, confirms spell `6673` appears in the spellbook, and confirms coinage decreases. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Trainer panel with synthetic spell rows and learn feedback. | Godot now scans live visible unit targets, selects the trainer by exact runtime GUID, opens a trainer list with clearer server-state rows, handles server-authoritative trainer-buy failure and success paths with learned-spell and money refresh proof, and displays a first resident trainer-window surface in the active world HUD. Remaining trainer parity includes feeding live trainer snapshots into the persistent HUD, real spell names/icons/ranks from the local-only data/asset pipeline, richer disabled-state explanations, normal in-world click selection, player-driven learn controls, and persistent-session integration. |
| Spells | Working expanded slice | `SMSG_INITIAL_SPELLS` parser, initial spell IDs, cooldown rows present in packet, `CMSG_CAST_SPELL`, empty-target casts, unit-target casts, `SMSG_SPELL_START`, `SMSG_SPELL_GO`. | `scenes/stage16_spellbook_view.tscn` with spell list and `scenes/stage16_spell_cast_view.tscn` with no-target and unit-target cast tests. `scenes/world_session_view.tscn` now has a resident Spells panel that can render safe session spellbook dictionaries without calling the bridge. | `ACORE_SPELLBOOK_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_spellbook_view.tscn` returns 48 initial spells. `ACORE_SPELL_CAST_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_spell_cast_view.tscn` casts spell `2457` and receives opcode `0x132`. `ACORE_TARGETED_SPELL_CAST_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_spell_cast_view.tscn` casts spell `78` at a live nearby creature and receives opcode `0x131`. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Spells panel with synthetic spellbook rows. | Read-only spellbook, safe no-target casting, live unit-target casting, and a resident world-session Spells surface work. Remaining spell parity includes feeding live spell snapshots into the persistent HUD, names/icons/ranks, item/destination/string targets, cooldown update packets, interrupt/fail messages, auras, damage/healing consequences, and combat results. |
| Action bars | Working expanded slice | `SMSG_ACTION_BUTTONS` parser, 144 packed action slots, action id/type unpacking, spell-slot casting through `CMSG_CAST_SPELL`, controlled `CMSG_SET_ACTION_BUTTON` set/restore probe. | `scenes/stage16_action_bar_view.tscn` with all 144 slots, populated-slot details, clickable spell slots, target controls, and a reversible set-slot probe. `scenes/world_session_view.tscn` now has a 12-slot in-world HUD action strip, an independent movable/resizable Actions panel ready to receive live slot data, and a Bag shortcut that opens the resident Bags panel. | `ACORE_ACTION_BAR_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_action_bar_view.tscn` returns 144 slots and 3 populated slots for `Codexstage`. `ACORE_ACTION_BAR_CAST_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_action_bar_view.tscn` casts server action button `73` spell `78` and receives opcode `0x131`. `ACORE_ACTION_BAR_SET_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_action_bar_view.tscn` sets slot `0` to spell `78`, confirms it, restores the original empty slot, and confirms the restore. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the world-session action strip, Bag shortcut routing, and multiple in-world panel creation. `ACORE_WORLD_SESSION_LAYOUT_SELF_TEST=1` validates separate persistent Options and Actions panel positions and sizes. | Server-provided action buttons display, one populated spell button can cast, and a controlled set/restore edit works through the Godot client path. The world-session HUD now has resident action and bag surfaces with persisted panel position and size. Remaining action-bar parity includes live slot data in the persistent HUD, names/icons, drag/drop placement, keybinds, macros, items, equipment sets, paging, and non-spell actions. |
| Auras | Working first UI slice | Aura update fields, buff/debuff display, durations, cooldowns, unit health/power, and selected-target state. | `scenes/world_session_view.tscn` now has a resident Auras panel that can render safe player and selected-target health, power, level/class, reaction/faction, buff/debuff/generic aura rows, and cooldown-like rows without calling the bridge. The selected-target HUD frame can also show safe target health/power and aura counts. | `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Auras panel and target frame with synthetic player health/power, player buff/debuff rows, cooldown rows, selected-target health, and target debuff rows. | Remaining aura/unit-frame parity includes feeding live update-field health/power/aura/cooldown snapshots into the persistent HUD, normal unit frames, buff/debuff icons/timers, dispel/cancel affordances, combat health deltas, death/ghost state, target-of-target, party/raid frames, and local-only spell metadata/icons/tooltips. |
| Groups | Working first UI slice | Invite/accept/leave, party member updates, loot settings. | `scenes/world_session_view.tscn` now has a resident Social panel that can render safe session party/group rows, party leader, roles, online state, and pending invites without calling the bridge. | `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Social panel with synthetic party and invite rows. | Remaining group parity includes feeding live party snapshots into the persistent HUD, player-driven invite/accept/decline/leave controls, party frames, loot settings, party chat integration, and a second local test character/session. |
| Guilds | Working first UI slice | Guild roster, invite, chat, rank permissions. | The resident world-session Social panel can render safe session guild rows, guild name, rank, online state, notes, and GUID details without calling the bridge. | `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Social panel with synthetic guild rows. | Remaining guild parity includes feeding live guild snapshots into the persistent HUD, roster paging/filtering, rank/permission actions, guild invite/remove/promote/demote controls, guild chat integration, and careful local-only state changes. |
| Mail | Working first UI slice | Mailbox list/read/send/delete, attachments, money, COD, mailbox object discovery. | `scenes/world_session_view.tscn` now has a resident Mail panel that can render safe session message rows, sender, subject, preview text, unread state, attached money, COD amounts, and attachment summaries without calling the bridge. | `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Mail panel with synthetic message, unread, COD, and attachment rows. | Remaining mail parity includes feeding live mailbox snapshots into the persistent HUD, mailbox object discovery, player-driven read/send/delete/return controls, attachment pickup, COD payment, full message body display, local-only item metadata/icons/tooltips, and a second local test character/session for send/receive coverage. |
| Auction house | Complete | Browse/search/bid/buyout/sell packets. | Auction house browser. | Browse auction listings from Godot. | Later-stage high-complexity UI/protocol work. |
| Maps/minimap | Complete | Map position, zone/area, POIs, discovered areas. | Map and minimap. | Show current map/zone and player marker. | Visual fidelity depends on local-only asset/data pipeline. |
| Addon-like UI customization | Working expanded slice | Saved UI layout/config only; not Lua compatibility initially. | `scenes/ui_customizer_view.tscn` provides movable sample frames, and `scenes/world_session_view.tscn` now has multiple independent movable/resizable persistent HUD panels including Bags, with reset support. | `ACORE_UI_LAYOUT_SELF_TEST=1 godot-4 --headless --path . res://scenes/ui_customizer_view.tscn` validates sample frame persistence. `ACORE_WORLD_SESSION_LAYOUT_SELF_TEST=1 godot-4 --headless --path . --scene res://scenes/world_session_view.tscn` validates separate world-session panel layout save/load/reset cleanup for position and size. `ACORE_WORLD_SESSION_SELF_TEST=1` validates the in-session Bags panel can coexist with other HUD panels. | World-session panel position and size persistence now exists in the actual play surface across independent panels, and Bags has joined the resident HUD surface set. Remaining UI customization parity includes profile/per-character scope decisions and full addon API compatibility as a separate major effort. |
| Item Tooltips | Complete | Quality color tiers (Poor through Heirloom), stat lines, socket/gem displays, equip effects, damage/DPS, armor, flavor text, and item level. | `scenes/tooltip_view.tscn` with hover-triggered tooltip overlays for sample WotLK items including Shadowmourne, Quel'Delar, and Invincible's Reins. | `ACORE_TOOLTIP_SELF_TEST=1 godot-4 --headless --path . res://scenes/tooltip_view.tscn` verified tooltip rendering and hide/show toggling for all quality tiers. | Reusable tooltip component ready for integration with inventory/equipment views. |
| Death/Respawn | Complete | Health tracking, death trigger, release spirit timer (6 min), ghost mode, graveyard teleport, corpse run distance, respawn at corpse with 50% HP and durability loss, resurrection offer/accept/decline with 35% HP revive. | `scenes/death_respawn_view.tscn` with death overlay panel, release/respawn buttons, resurrection dialog, and simulation controls. | `ACORE_DEATH_RESPAWN_SELF_TEST=1 godot-4 --headless --path . res://scenes/death_respawn_view.tscn` verified full death lifecycle including damage, death state, release, ghost mode, corpse respawn, and resurrection accept/decline. | Needs integration with live server SMSG_DEATH packets and corpse position from update fields. |
| Settings | Working expanded slice | Shared settings runtime, window resolution, fullscreen/windowed mode, VSync, master/music/SFX/ambience volume, gameplay toggles, broader sandbox/world-session keybindings, persisted config, runtime `InputMap` application, and gameplay-scene keybind consumption. | `scenes/settings_view.tscn` with Video, Audio, Gameplay, and Keybindings tabs, reachable from the dashboard. `scenes/gameplay_sandbox.tscn` and `scenes/world_session_view.tscn` apply saved keybindings on startup and use actions instead of raw WASD/F/1/R or Godot UI-arrow keys. | `ACORE_SETTINGS_SELF_TEST=1 godot-4 --headless --path . res://scenes/settings_view.tscn` mutates a temporary settings file, reloads it, applies it, verifies the keybind in `InputMap`, and removes the temporary file. `ACORE_SANDBOX_KEYBIND_SETTINGS_SELF_TEST=1 godot-4 --headless --path . res://scenes/gameplay_sandbox.tscn` verifies the sandbox consumes saved movement bindings. `ACORE_WORLD_SESSION_KEYBIND_SELF_TEST=1 godot-4 --headless --path . --scene res://scenes/world_session_view.tscn` verifies the world-session shell consumes saved movement, camera, target, action, interact, reset, and jump bindings. | Godot now has a first options menu with saved preferences, shared runtime application, and playable UI scenes consuming saved keybinds. Remaining settings parity includes mouse/camera sensitivity values, UI scale, combat text/chat options, per-character/account scope decisions, and wiring the settings into server-synchronized persistent world HUD/camera/input systems. |

## Compatibility Rules

- Prefer exact AzerothCore/WotLK protocol behavior where possible.
- When Godot requires a technical adaptation, document it as a compatibility deviation.
- Keep proprietary client assets, extracted files, converted derivatives, secrets, account config, and generated runtime data out of Git.
- Use local AI models only as bounded advisory reviewers for non-sensitive snippets; the project checks remain the source of truth.
- Update this matrix whenever a feature moves from planned to in progress, working prototype, complete, blocked, or explicitly deferred.

## Stage 17 Gate Link

Stage 17 can only be treated as reached when this matrix shows the normal client feature surface is implemented or has explicit, justified compatibility notes, and Godot can serve as the player-facing client for ordinary AzerothCore play without launching the original client.
