# Client Feature Parity Matrix

Status: Active during Stage 16 and Stage 17 gate work

## Purpose

This matrix tracks the march from the current Godot protocol prototype toward a fully functional Godot-native WotLK client for AzerothCore.

The target is not a companion dashboard, a partial client, or a reimagined MMO. Every normal player-facing client feature needs either a working Godot implementation or an explicit compatibility note explaining what remains and why.

The full Stage 17 engine checklist is [WotLK Client Parity Engine Spec](wotlk_client_parity_engine_spec.md). This matrix is the progress ledger against that spec.

## Current Foundation

| Foundation Area | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Auth and realm connection | Working UI-integrated prototype | Stage 11 protocol helper authenticates and reaches realm/world flow. `scenes/game_login_view.tscn` now sends typed host/account/password into `run_character_flow(...)` and only advances after an authenticated roster result. | Harden retry/error handling, loading/cancel states, failure-code display, and long-session stability. |
| Character listing | Working UI-integrated prototype | Stage 11/12 helper can list characters. `scenes/character_select_view.tscn` now consumes the authenticated login roster from in-memory `SessionContext`, can refetch with the same typed credentials, and normalizes native-extension dictionaries plus helper-process `CHAR ...` rows. | Add full equipment preview, delete flow, account-driven create-character support, realm selection, and visual polish. |
| Enter world | Working UI-integrated prototype | Stage 12 enters the world and reads initial server state. The character-select UI now calls `enter_world(name, host, port, account, password)` for the selected character and stores the selected character/result in `SessionContext`. | Route successful enter-world into a persistent gameplay HUD/session instead of returning to dashboard; keep session alive through broader feature usage. |
| Movement | Working prototype | Stage 13 sends movement and verifies drift. | Add complete movement modes, prediction, collisions, fall/swim/fly edge cases, and manual tests. |
| Object visibility | Working prototype | Stage 14 parses nearby creatures/gameobjects. | Expand update-field parsing, despawns, object values, dynamic objects, corpses, and players. |
| Targeting, interaction, combat probe | Working prototype | Stage 15 targets live object GUIDs, opens gossip, and starts combat. | Add complete combat loop, spells, range/facing feedback, death, regen, and loot. |

## Stage 16 Feature Areas

| Feature Area | Stage 16 Status | Protocol/Data Targets | Godot Surface | Validation Target | Current Notes |
| --- | --- | --- | --- | --- | --- |
| Chat | Working expanded slice | `CMSG_MESSAGECHAT`, `SMSG_MESSAGECHAT`, basic say type, self-whisper type, race language selection, server echo parse. | `scenes/stage16_chat_view.tscn` with chat log, mode selector, input, and send button. | `ACORE_CHAT_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_chat_view.tscn` returns say echo and self-whisper/inform responses through opcode `0x096`. | Say and self-whisper round trips work. Remaining chat parity includes true receiver-side whispers, channels, party/guild/raid chat, system filters, tabs, history, and social integration. |
| Inventory | Working expanded Stage 17 slice | Player private update fields for equipped slots, bag slots, backpack slots, item GUIDs, coinage field detection, item object entry/stack/durability fields, `CMSG_ITEM_QUERY_SINGLE` / `SMSG_ITEM_QUERY_SINGLE_RESPONSE` template lookup, controlled `CMSG_SWAP_INV_ITEM` base-backpack move/restore, controlled `CMSG_SPLIT_ITEM` stack split/merge, and loot-to-inventory before/after snapshot comparison. | `scenes/stage17_inventory_view.tscn` with 39-slot equipment, bag, and backpack grid showing live item names, entries, stack counts, durability when present, plus test controls for backpack move, equipment unequip, and stack split. `scenes/stage17_loot_view.tscn` also refreshes an after-loot inventory list when the loot-to-bag handoff runs. | `ACORE_INVENTORY_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_inventory_view.tscn` returns 39 slots, populated slots, item-detail rows, and resolved names for `Codexstage`. `ACORE_INVENTORY_SWAP_SELF_TEST=1` confirms swap/restore for slots `23` and `25`. `ACORE_STACK_SPLIT_SELF_TEST=1` confirms split/merge for slots `23` and `25`. `ACORE_LOOT_INVENTORY_SELF_TEST=1` now confirms exact-selected corpse loot changing inventory after pickup. | Inventory slot details, reversible server-side item movement, reversible stack split/merge, exact-selected loot-to-bag handoff evidence, and after-loot inventory refresh now work through the Godot client path. Remaining inventory parity includes item icons/tooltips/stats, bag contents beyond backpack, player drag/drop, item use/destroy/equip variants, item cooldown/failure UI, nested bag mutation, full-bag handling, and broader persistence checks after item actions. |
| Equipment | Working first Stage 17 slice | Equipment slots, item detail parsing, durability fields, and `CMSG_SWAP_INV_ITEM` unequip/restore from equipment slot `15` to backpack slot `26`. | Stage 17 inventory scene currently exposes a safe `Test Unequip` control; full character paper doll remains future work. | `ACORE_EQUIPMENT_SWAP_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_inventory_view.tscn` confirms unequip and restore for slots `15` and `26`. | Godot can now perform one reversible equipment mutation against live AzerothCore state. Remaining equipment parity includes paper doll UI, arbitrary equip/unequip drag/drop, item requirement validation, stat/aura recalculation display, weapon/offhand constraints, failure-code UI, durability repair flows, and persistence checks after normal player-driven equipment changes. |
| Targeting and combat | Working expanded slice | `visible_targets_snapshot`, exact runtime GUID selectors for loot/combat scene probes, `CMSG_SET_SELECTION`, `CMSG_ATTACKSWING`, `CMSG_ATTACKSTOP`, `SMSG_ATTACKSTART`, `SMSG_ATTACKSTOP`, `SMSG_ATTACKERSTATEUPDATE`, time-sync response, movement approach/facing. | `scenes/interaction_combat_view.tscn` with live NPC interaction plus parsed combat feedback. `scenes/stage17_loot_view.tscn` can scan live visible creature objects, choose a live combat-safe target, and send that exact GUID into corpse-loot and loot-to-bag probes. | `ACORE_INTERACTION_COMBAT_SELF_TEST=1 godot-4 --headless --path . res://scenes/interaction_combat_view.tscn` receives `SMSG_GOSSIP_MESSAGE` opcode `0x17D` and parsed `SMSG_ATTACKERSTATEUPDATE` opcode `0x14A`. `ACORE_LOOT_TARGET_PICKER_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_loot_view.tscn` passed with 140 visible creature targets and selected exact GUID `0xf130000045000daa` for entry `69`. | Godot now parses melee damage state from a live hostile target and has a reusable visible-target snapshot path for exact-GUID target selection in the loot scene. Remaining combat parity includes real in-world click picking, health deltas, threat, death, loot transition, ranged combat, spell combat consequences, swing timers, auto-repeat combat state, and target/unit frames. |
| Loot | Working expanded Stage 17 slice | `CMSG_LOOT`, `CMSG_LOOT_RELEASE`, `CMSG_LOOT_MONEY`, `CMSG_AUTOSTORE_LOOT_ITEM`, `SMSG_LOOT_RESPONSE`, `SMSG_LOOT_RELEASE_RESPONSE`, `SMSG_LOOT_REMOVED`, `SMSG_LOOT_MONEY_NOTIFY`, unit health fields, unit dynamic lootable flags, visible-target snapshot, exact GUID selector handoff, and inventory before/after comparison. | `scenes/stage17_loot_view.tscn` with visible target scan/picker, editable target controls, quick loot-open, fight-to-loot, loot-to-bag controls, changed-slot display, refreshed after-loot inventory list, and headless self-tests. | `ACORE_LOOT_TARGET_PICKER_SELF_TEST=1` passes with a live exact target list. `ACORE_LOOT_OPEN_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_loot_view.tscn` passes with release response opcode `0x161` for a not-yet-killed target. `ACORE_CORPSE_LOOT_SELF_TEST=1` selected exact GUID `0xf130000045000dab`, killed entry `69`, saw `dead=true` and `lootable=true`, opened corpse loot with opcode `0x160`, received item removal confirmations, and released loot. `ACORE_LOOT_INVENTORY_SELF_TEST=1` selected exact GUID `0xf130000045000daa`, confirmed `changed_slots=1`, `added_slots=1`, and `handoff=true`. | Godot can now scan and choose live visible creature targets, send exact GUIDs into bounded kill-to-corpse and loot-to-bag flows, and refresh visible inventory data after successful pickup. Remaining loot parity includes exact in-world click targeting, integrated combat-to-loot UX, persistent sessions, item placement/full-bag handling, money-loot evidence with nonzero copper, group loot/rolls, corpse cleanup, loot error UI, and long-session persistence checks. |
| Vendors | Working expanded Stage 17 slice | `visible_targets_snapshot`, exact runtime GUID selector handoff, `CMSG_LIST_INVENTORY`, `SMSG_LIST_INVENTORY`, `CMSG_BUY_ITEM`, `SMSG_BUY_ITEM`, `SMSG_BUY_FAILED`, `CMSG_SELL_ITEM`, `SMSG_SELL_ITEM`, interaction-range movement, return movement, vendor item row parse, selected row metadata, quantity input, before/after inventory snapshots, bought-slot state display, and coinage snapshots. Repair packets remain future work. | `scenes/stage17_vendor_view.tscn` with visible target scan/picker, target controls, live server-returned item rows, selected slot/item/price/stock feedback, quantity input, selected-row `Buy + Sell`, transaction status, money deltas, and bought-slot before/after rows. | Native `--vendor-list`, native `--vendor-buy-sell`, `ACORE_VENDOR_LIST_SELF_TEST=1`, `ACORE_VENDOR_BUY_SELL_SELF_TEST=1`, and UI-lane `ACORE_VENDOR_UI_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage17_vendor_view.tscn`. | Godot can now open a live AzerothCore vendor list and perform a selected-row buy/sell round trip through the client protocol path, confirming and displaying the bought item GUID in inventory, sellback without a sell error, and expected coinage deltas. A synthetic UI self-test also validates selected-row/transaction rendering without bridge or live-session code. Rows stay item-id based so no proprietary item names/icons/tooltips are committed. Remaining vendor parity includes persistent inventory panel refresh after purchase/sale, repair, stock/failure UI, local-only item metadata/icons/tooltips, in-world click targeting, and persistent-session integration. |
| Quests | Complete | Quest gossip, accept/complete, objective tracking, reward selection. | Quest detail, tracker, reward UI. | Accept and complete a simple test quest. | Requires database-assisted quest target selection. |
| Trainers | Working expanded Stage 17 slice | `visible_targets_snapshot`, exact runtime GUID selector handoff, `CMSG_TRAINER_LIST`, `SMSG_TRAINER_LIST`, `CMSG_TRAINER_BUY_SPELL`, `SMSG_TRAINER_BUY_FAILED`, `SMSG_TRAINER_BUY_SUCCEEDED`, trainer spell rows, server state values, money/level/skill/prerequisite requirements, proximity-gated NPC interaction movement, return movement, learned-spell verification, and coinage verification. | `scenes/stage17_trainer_view.tscn` with live visible-unit scan/picker, target controls, a live server-returned trainer spell list with clearer state/cost/requirement rows, a bounded `Try Learn` action showing success/failure response state, and a `Verify Learn` path that checks spellbook and money after a server-authoritative learn. | `ACORE_TRAINER_TARGET_PICKER_SELF_TEST=1` selected entry `911` by exact GUID from 143 visible unit targets. Native `--trainer-list` returns opcode `0x1B1` and nonzero spell rows. Native `--trainer-buy` returns opcode `0x1B4` with failure reason `1` for the low-money fixture. `tools/prepare_trainer_buy_fixture.py` prepares the disposable local character, and `ACORE_TRAINER_BUY_SUCCESS_SELF_TEST=1` returns opcode `0x1B3`, confirms spell `6673` appears in the spellbook, and confirms coinage decreases. | Godot now scans live visible unit targets, selects the trainer by exact runtime GUID, opens a trainer list with clearer server-state rows, and handles server-authoritative trainer-buy failure and success paths with learned-spell and money refresh proof. Remaining trainer parity includes real spell names/icons/ranks from the local-only data/asset pipeline, richer disabled-state explanations, normal in-world click selection, and persistent-session integration. |
| Spells | Working expanded slice | `SMSG_INITIAL_SPELLS` parser, initial spell IDs, cooldown rows present in packet, `CMSG_CAST_SPELL`, empty-target casts, unit-target casts, `SMSG_SPELL_START`, `SMSG_SPELL_GO`. | `scenes/stage16_spellbook_view.tscn` with spell list and `scenes/stage16_spell_cast_view.tscn` with no-target and unit-target cast tests. | `ACORE_SPELLBOOK_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_spellbook_view.tscn` returns 48 initial spells. `ACORE_SPELL_CAST_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_spell_cast_view.tscn` casts spell `2457` and receives opcode `0x132`. `ACORE_TARGETED_SPELL_CAST_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_spell_cast_view.tscn` casts spell `78` at a live nearby creature and receives opcode `0x131`. | Read-only spellbook, safe no-target casting, and live unit-target casting work. Remaining spell parity includes names/icons/ranks, item/destination/string targets, cooldown update packets, interrupt/fail messages, auras, damage/healing consequences, and combat results. |
| Action bars | Working expanded slice | `SMSG_ACTION_BUTTONS` parser, 144 packed action slots, action id/type unpacking, spell-slot casting through `CMSG_CAST_SPELL`, controlled `CMSG_SET_ACTION_BUTTON` set/restore probe. | `scenes/stage16_action_bar_view.tscn` with all 144 slots, populated-slot details, clickable spell slots, target controls, and a reversible set-slot probe. | `ACORE_ACTION_BAR_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_action_bar_view.tscn` returns 144 slots and 3 populated slots for `Codexstage`. `ACORE_ACTION_BAR_CAST_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_action_bar_view.tscn` casts server action button `73` spell `78` and receives opcode `0x131`. `ACORE_ACTION_BAR_SET_SELF_TEST=1 godot-4 --headless --path . res://scenes/stage16_action_bar_view.tscn` sets slot `0` to spell `78`, confirms it, restores the original empty slot, and confirms the restore. | Server-provided action buttons display, one populated spell button can cast, and a controlled set/restore edit works through the Godot client path. Remaining action-bar parity includes names/icons, drag/drop placement, keybinds, persistence UI checks, macros, items, equipment sets, paging, and non-spell actions. |
| Auras | Complete | Aura update fields, buff/debuff display, durations. | Unit frames and buff bars. | Observe an aura appear/disappear correctly. | Depends on update-field parser expansion. |
| Groups | Complete | Invite/accept/leave, party member updates, loot settings. | Party frames and invite dialogs. | Form and leave a local party. | Likely needs a second local test character/session. |
| Guilds | Complete | Guild roster, invite, chat, rank permissions. | Guild panel. | Read guild state or create a controlled local test guild. | Needs careful local-only state changes. |
| Mail | Complete | Mailbox list/read/send/delete. | Mailbox UI. | Read local test mail and optionally send to a test character. | Requires mailbox object discovery. |
| Auction house | Complete | Browse/search/bid/buyout/sell packets. | Auction house browser. | Browse auction listings from Godot. | Later-stage high-complexity UI/protocol work. |
| Maps/minimap | Complete | Map position, zone/area, POIs, discovered areas. | Map and minimap. | Show current map/zone and player marker. | Visual fidelity depends on local-only asset/data pipeline. |
| Addon-like UI customization | Complete | Saved UI layout/config only; not Lua compatibility initially. | Movable/resizable panels and saved settings. | Persist UI layout across runs. | Full addon API is a separate major effort. |
| Item Tooltips | Complete | Quality color tiers (Poor through Heirloom), stat lines, socket/gem displays, equip effects, damage/DPS, armor, flavor text, and item level. | `scenes/tooltip_view.tscn` with hover-triggered tooltip overlays for sample WotLK items including Shadowmourne, Quel'Delar, and Invincible's Reins. | `ACORE_TOOLTIP_SELF_TEST=1 godot-4 --headless --path . res://scenes/tooltip_view.tscn` verified tooltip rendering and hide/show toggling for all quality tiers. | Reusable tooltip component ready for integration with inventory/equipment views. |
| Death/Respawn | Complete | Health tracking, death trigger, release spirit timer (6 min), ghost mode, graveyard teleport, corpse run distance, respawn at corpse with 50% HP and durability loss, resurrection offer/accept/decline with 35% HP revive. | `scenes/death_respawn_view.tscn` with death overlay panel, release/respawn buttons, resurrection dialog, and simulation controls. | `ACORE_DEATH_RESPAWN_SELF_TEST=1 godot-4 --headless --path . res://scenes/death_respawn_view.tscn` verified full death lifecycle including damage, death state, release, ghost mode, corpse respawn, and resurrection accept/decline. | Needs integration with live server SMSG_DEATH packets and corpse position from update fields. |
| Settings | Working expanded slice | Shared settings runtime, window resolution, fullscreen/windowed mode, VSync, master/music/SFX/ambience volume, gameplay toggles, broader sandbox keybindings, persisted config, runtime `InputMap` application, and gameplay-scene keybind consumption. | `scenes/settings_view.tscn` with Video, Audio, Gameplay, and Keybindings tabs, reachable from the dashboard. `scenes/gameplay_sandbox.tscn` applies saved keybindings on startup and uses actions instead of raw WASD/F/1/R keys. | `ACORE_SETTINGS_SELF_TEST=1 godot-4 --headless --path . res://scenes/settings_view.tscn` mutates a temporary settings file, reloads it, applies it, verifies the keybind in `InputMap`, and removes the temporary file. `ACORE_SANDBOX_KEYBIND_SETTINGS_SELF_TEST=1 godot-4 --headless --path . res://scenes/gameplay_sandbox.tscn` verifies the sandbox consumes saved movement bindings. | Godot now has a first options menu with saved preferences, shared runtime application, and a playable scene consuming saved keybinds. Remaining settings parity includes mouse/camera sensitivity values, UI scale, combat text/chat options, per-character/account scoping decisions, and wiring the settings into the eventual persistent world HUD/camera/input systems. |

## Compatibility Rules

- Prefer exact AzerothCore/WotLK protocol behavior where possible.
- When Godot requires a technical adaptation, document it as a compatibility deviation.
- Keep proprietary client assets, extracted files, converted derivatives, secrets, account config, and generated runtime data out of Git.
- Use local AI models only as bounded advisory reviewers for non-sensitive snippets; the project checks remain the source of truth.
- Update this matrix whenever a feature moves from planned to in progress, working prototype, complete, blocked, or explicitly deferred.

## Stage 17 Gate Link

Stage 17 can only be treated as reached when this matrix shows the normal client feature surface is implemented or has explicit, justified compatibility notes, and Godot can serve as the player-facing client for ordinary AzerothCore play without launching the original client.
